# Frontend #2 (Vite 3D) — set VITE_API_URL in the Cloudflare Pages project
# (Settings → Environment variables). Baked into the bundle at build time.
# NOTE the /api suffix — this app's API client prepends paths to it.

VITE_API_URL=https://api.yourdomain.com/api
